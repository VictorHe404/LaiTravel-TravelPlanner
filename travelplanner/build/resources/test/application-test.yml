spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/travelplanner_test
    username: planner
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: never
